# mobile_prices

to clone the repo run this in the command line
`git clone https://github.com/sivansh11/cellphones-linear-regression.git`


to install all python libraries run in the command line
`pip install -r requirements.txt`
